## UI Lint 工具 — 运行说明

### ui_lint.gd

```bash
cmd.exe /c "Godot_v4.6.2-stable_win64_console.exe --headless --path D:\...\slay-demo --script res://tools/ui_lint.gd"
```

### render_ui_preview.gd

截图需要窗口模式，在 Gallery 中点「保存+预览」或手动运行场景后截图。
